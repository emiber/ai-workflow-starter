# Architecture

> This file is filled in when you run `/init-project`. It documents the project's architecture decisions.

## Project type
_(pending)_

## Components
_(pending)_

## Tech stack
| Component | Chosen technology | Reason |
|---|---|---|
| | | |

## Quality gate
- **Coverage threshold:** 85% (default). Any change from 85% is an explicit decision recorded here with the reason.
- **Measured by:** _(pending — e.g. vitest coverage, pytest-cov)_
- Enforced identically in local scripts and in CI (`.github/workflows/ci.yml`).

## Deferred decisions
_(things we decided NOT to include now and why)_
